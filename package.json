{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "duckbuck",
  "scripts": {
    "build": "pnpm run build --workspaces --if-present",
    "prebuild": "pnpm run prebuild --workspaces --if-present",
    "pretest": "pnpm run prebuild --workspaces --if-present",
    "start": "pnpm start --workspaces --if-present",
    "test": "pnpm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/duckbuck_frontend"
  ]
}
